{
  "name": "tryambakkaushik-site",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "build": "astro build",
    "postbuild": "mkdir -p dist/assets && cp -R assets/. dist/assets && cp CNAME dist/CNAME",
    "preview": "astro preview"
  },
  "dependencies": {
    "astro": "^5.12.8"
  }
}
